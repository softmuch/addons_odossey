<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="OrderReceipt" t-inherit-mode="extension" t-inherit="point_of_sale.OrderReceipt">  
        <xpath expr="//div[contains(@class,'pos-receipt-taxes')]" position="replace">  
            <t t-set="to_split" t-value="props.data.to_split"/>
            <t t-set="split_done" t-value="props.data.to_split"/>
            <t t-if="props.data &amp;&amp; props.data.is_split &amp;&amp; split_done != to_split">
                <t t-call="PosReceiptTaxessplit"/>
            </t> 
            <t t-else="">
                <div t-if="taxTotals and taxTotals.has_tax_groups" class="pos-receipt-taxes"> 
                    <div t-if="props.data.is_split">
                        <div class="text-center"></div>
                        <div class=" text-start">
                            <span class="fw-bolder text-nowrap mw-100">Split Reciept Total</span>
                            <span t-esc="props.formatCurrency(props.data.amount_total)" class="pos-receipt-right-align"/>
                        </div>
                    </div>
                    <div class="text-center">--------------------------------</div>
                    <t t-foreach="taxTotals.subtotals" t-as="subtotal" t-key="subtotal.name">
                        <div class="d-flex">
                            <span t-if="showTaxGroupLabels" class="me-2" style="visibility: hidden;">A</span>
                            <span class="fw-bolder text-nowrap mw-100" t-out="subtotal.name"/>
                            <span t-esc="props.formatCurrency(subtotal.base_amount_currency)" class="ms-auto"/>
                        </div> 
                        <div t-foreach="subtotal.tax_groups" t-as="tax_group" t-key="tax_group.id" class="d-flex">
                            <t t-if="showTaxGroupLabels">
                                <span t-if="tax_group.group_label" t-out="tax_group.group_label" class="me-2"/>
                                <span t-else="" class="me-2" style="visibility: hidden;">A</span>
                            </t>
                            <span>
                                <span t-esc="tax_group.group_name"/>
                                <t t-if="!taxTotals.same_tax_base">
                                    on
                                    <span t-esc="props.formatCurrency(tax_group.base_amount_currency)"/>
                                </t>
                            </span>
                            <span t-esc="props.formatCurrency(tax_group.tax_amount_currency)" class="ms-auto"/>
                        </div>
                    </t>
                </div>
            </t>
        </xpath>
    </t>
    <t t-name="PosReceiptTaxessplit" owl="1">
        <div t-if="taxTotals and taxTotals.has_tax_groups" class="pos-receipt-taxes"> 
            <div class="text-center">--------------------------------</div>
            <t t-foreach="taxTotals.subtotals" t-as="subtotal" t-key="subtotal.name">
                <div class="d-flex">
                    <span t-if="showTaxGroupLabels" class="me-2" style="visibility: hidden;">A</span>
                    <span class="fw-bolder text-nowrap mw-100" t-out="subtotal.name"/>
                    <span t-esc="props.formatCurrency(subtotal.base_amount_currency/props.data.to_split)" class="ms-auto"/>
                </div>

                <div t-foreach="subtotal.tax_groups" t-as="tax_group" t-key="tax_group.id" class="d-flex">
                    <t t-if="showTaxGroupLabels">
                        <span t-if="tax_group.group_label" t-out="tax_group.group_label" class="me-2"/>
                        <span t-else="" class="me-2" style="visibility: hidden;">A</span>
                    </t>
                    <span>
                        <span t-esc="tax_group.group_name"/>
                        <t t-if="!taxTotals.same_tax_base">
                            on
                            <span t-esc="props.formatCurrency(tax_group.base_amount_currency/props.data.to_split)"/>
                        </t>
                    </span>
                    <span t-esc="props.formatCurrency(tax_group.tax_amount_currency/props.data.to_split)" class="ms-auto"/>
                </div>
            </t>
        </div>
    </t>
</templates>